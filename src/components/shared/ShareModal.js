import React from 'react';

const ShareModal = () => {
  // This would be a modal for sharing achievements
  // For now, it's just a placeholder that doesn't render anything
  return null;
};

export default ShareModal;